{
  "name": "lanzou-direct-link-worker",
  "version": "1.0.0",
  "description": "蓝奏云直链解析服务 - Cloudflare Worker 版本",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --port=8787 --ip=0.0.0.0",
    "dev:docker": "docker-compose up --build",
    "dev:docker:debug": "docker-compose up --build worker",
    "dev:container": "docker run -it --rm -p 8787:8787 -v $(pwd):/workspace node:20-bullseye bash -c \"cd /workspace && npm install && npm run dev\""
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.32.0"
  },
  "keywords": ["lanzou", "direct-link", "api"],
  "author": "",
  "license": "MIT"
}